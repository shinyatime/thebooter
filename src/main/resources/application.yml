spring:
  datasource:
    url: jdbc.mysql://localhost:3306/thebooter?characterEncoding=utf8
    username: root
    username: root
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect